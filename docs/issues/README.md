# Historical implementation briefs

These numbered files are archived implementation briefs, not the current
product or architecture specification. The repository README, `PRODUCT.md`,
accepted ADRs, tests, and `docs/manual-integration-gate.md` are authoritative.

- `0001` and `0008`–`0010` describe a shadow-agent design superseded by ADR
  0009.
- `0012` was implemented with child-message model override and session-model
  persistence instead of per-call shadow agents.
- `0002`–`0007`, `0011`, and `0013`–`0016` are implemented.
- `0017` remains the live, fail-closed release gate.
